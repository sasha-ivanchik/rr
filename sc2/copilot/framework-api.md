# Framework API (Reference Only)

## Dropdown
- setDropdown(page, label, values, options)

## Grid
- waitForRow(page, { column, value })
- getCellValue(page, column, rowIndex)

## Modal
- waitOpen()
- closeSafely()

## Utils
- waitForStableUI()
- retry(action, options)
